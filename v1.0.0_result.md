# v1.0.0 実装結果 — レビューとの答え合わせ

作成日: 2026-07-20
対象: `v1.0.0.md`(コンセプト)/`v1.0.0_review.md`(事前レビュー)/`AI/specs/v1.0.0_implementation_plan.md`(実装計画 S0〜S7)
実装: dev ブランチのコミット S0〜S7(0.12.0 → 1.0.0)。テスト58件通過・`dart analyze` クリーン・`dart pub publish --dry-run` 通過。

---

## 1. コンセプト(v1.0.0.md)の各項目がどうなったか

| コンセプト | 結果 | 実装内容 |
|---|---|---|
| §2 `utakata.yaml` マスター化 | ✅ 実装(0.12.0) | `doc init` が `schema: 1` + コメント例付きテンプレートを生成。全コマンドが参照し、`project.architecture` は plan.yaml より優先(食い違いは警告)。`doctor` がスキーマ検証 |
| §2-A ナレッジ外部化 | ✅ 実装(0.13.0/0.14.0) | レビューでの軌道修正どおり「デフォルト同梱・オプトインでリモート」。`utakata_arch_lib` が Single Source of Truth になり、リリース時に `tool/sync_arch_lib.dart` で同梱コピーを再生成。プロジェクトへのナレッジ丸ごとコピー(旧 `AI/`)は**完全廃止**され、参照型(`guide show/eject`・MCP)へ移行 |
| §2-B 登場人物(team)定義 | ✅ 実装(0.12.0) | `team:`(client/developer/ai_agents)を `utakata.yaml` に定義。`create` が生成する `CLAUDE.md` に「登場人物と役割」節として出力され、MCP `config_get` でも機械可読に取得できる |
| §2-C SKILLs の動的解決・自動追記 | ✅ 実装(0.15.0) | `utakata skills sync [--force]`。ただし「自動追記」ではなく**明示コマンドによる同期**に変更(理由は後述の懸念②) |
| §3 feature 別実装計画 | ✅ 実装済みだった | 再設計(旧0.9.0)の `utakata impl` がコンセプトの記述とほぼ一致しており、追加実装は不要だった |
| §4 ビジネスログの構造化 | ✅ 実装済みだった | 同上(旧0.8.0/0.9.0 の `log`/`agree` + deny ルール)。human-write-only の技術的強制も既存のまま維持 |
| §5 複数AIオーケストレーション | ⏸ 見送り(D8) | 実行機構は作らず、`team.ai_agents` の**定義と公開**(CLAUDE.md/config_get)まで。実案件での必要性が実証されてから |
| ロードマップA(arch_lib 構築) | ✅ 実装(S2a) | `arches/<id>/{arch_definition.yaml, principles/, layers/, dependencies/, skills/}` に確定。`.tmpl` 全廃、mvvm のレガシー(`.agent/`・`AI/`)一掃、`_starter`(独自アーキテクチャ雛形)+ HOW_TO_ADD.md 追加 |
| ロードマップB(フェッチエンジン) | ✅ 実装(0.14.0) | `utakata arch get [--update]`。`~/.utakata/cache/knowledge/` にキャッシュし、**コミット SHA を `utakata.lock` に固定** |
| ロードマップC(MCP拡張+lint→ガイド) | ✅ 実装(0.16.0) | `utakata guide for <file>` + MCP `guide_for_file`/`config_get`。lint エラーのファイルパスから該当層ガイドを決定論的に解決 |
| ロードマップD(公式ログ同期) | ✅ 実装(0.17.0) | `utakata log import claude-session`。レビューで確定した「人間駆動・レダクション・確認プロンプト」の形 |
| ロードマップD後段(コマンド白紙化) | ⏸ 見送り(D8) | スコープ限定の設計が先、という判断のまま |

---

## 2. レビューの懸念①〜⑥はどう決着したか

| 懸念 | 決着 |
|---|---|
| **① リモートフェッチの飛躍**(オフライン・注入・再現性) | レビュー追記(2026-07-18)の軌道修正が最終形になった。デフォルトは同梱(pub cache でオフライン完結)、リモートは `knowledge_repo` 明示時のみ。さらに `utakata.lock` の SHA 固定で「差し替えられた知識を無条件に信じる」経路と再現性喪失を同時に塞いだ。`--update` 時は新旧 SHA を表示して確認を促す |
| **② SKILLs 自動追記の競合設計欠如** | managed マーカー方式(D4)で解決。マーカーの無いファイル(人間作)は **`--force` でも絶対に上書きしない**。未編集 managed は更新、編集済み managed はスキップ(`--force` でのみ上書き)、リストから外れたものは削除候補の報告のみ。この3規則すべてにテストがある |
| **③ 複数AI編成に実装計画が無い** | 予定どおり見送り(D8)。`team` 定義という「座組の記述」までを実装し、ランタイムは作らなかった |
| **④ `.tmpl` 廃止は1バージョン分の規模** | S2 として独立実施。事前調査で「CLI は既に `.dart` プレースホルダー生成をスキップ済み=廃止コストが低い」と判明し、懸念より小さく済んだ。mvvm は「構造は新形式・文章は流用」の縮小版で確定(リスク欄の逃げ道どおり) |
| **⑤ arch_lib が再設計前の内容のコピー** | S2a で全面解消。旧 `.agent/`・`feature_request.yaml`・`generate_feature.sh` 前提の記述はゼロになり、arch_definition.yaml は CLI 側の最新版(命名規則修正入り)を正として取り込んだ。レビュー追記どおり Clean/MVVM/`_starter` の3本構成 |
| **⑥ 公式ログ同期のプライバシー境界** | 二転三転の末に確定した「生トランスクリプトを doc/records/ に Git 管理で取り込みたい」を実装。3つの未解決論点はそれぞれ: (a) レダクション → 秘密情報パターンの `[REDACTED]` 置換+件数報告+確認プロンプト、(b) ノイズ → 既定で user/assistant テキストのみ(thinking・tool・サブエージェント除外、tool_result は `--full` でも除外)、(c) 保存形式 → 正規化 JSONL(`{ts, role, text, session, seq}`)+ Markdown プレビュー併置。プロジェクト分離は Claude Code 側のディレクトリ規則(`projectKeyOf`)をそのまま利用 |

---

## 3. レビュー「推奨する進め方」との対応

1. 「`team`/`skills` を utakata.yaml に足す(今すぐ)」 → **S1 で最初に実施**(0.12.0)
2. 「arch_lib の内容同期がフェッチ実装より先」 → **そのとおり S2a → S3 の順で実施**。フェッチ対象が正しくなってからエンジンを作った
3. 「リモートフェッチはバージョン固定付き最小実装で切り出す」 → **S3 がまさにその形**(lock + SHA、チェックサムは git の SHA 自体が担う)
4. 「ログ同期は3論点を決めてから」 → **S6 で3論点すべてに決着をつけて実装**

レビュー時の判断が覆った点は1つ: 「Claude Code の auto memory があるから生ログ同期は優先度を下げてよい」という一時的な示唆は、ユーザーの再訂正(目的は文脈供給ではなく**監査証跡としての Git 管理**)により撤回し、実装する方に倒した。

---

## 4. 実装で新たに判明したこと(レビュー時点では見えていなかったもの)

- **`.tmpl` は既にほぼ死んでいた**: 0.5.1 以降 CLI が `.dart` プレースホルダー生成をスキップしていたため、廃止は「重い作り直し」ではなく「死荷重の削除」だった。
- **ContentHash の符号バグ**: managed マーカーのハッシュが Dart の符号付き64bit整数で負になると `-` 付き16進になり、マーカー正規表現に一致せず「未管理ファイル」と誤判定される。テストが検出し符号なし整形で修正(0.15.0)。
- **リモートキャッシュの「materialize」方式**: フェッチしたリポジトリを同梱テンプレートと同じ `architectures/<id>/` 形式に変換してキャッシュすることで、解決コードが「リモート→同梱」を単一のパス空間で扱えた。同期スクリプトと同じ変換規則を共有している。
- **`create` は `utakata.yaml` を生成しない**(仕様書 §17 の未解決課題のまま)。`doc init` が唯一の生成元。create 時の自動生成は今後の小改良候補。

---

## 5. 残っている作業

**ユーザーの操作待ち(外部公開)**:
- `utakata_arch_lib` の `restructure` ブランチ push → main マージ → `v1.0.0` タグ
- utakata 本体 dev の push → main マージ
- `dart pub publish`(pub.dev への公開)

**意図的に残した負債(小)**:
- 新コマンド(`arch get`/`skills sync`/`guide for`/`log import`)のメッセージは日本語ハードコード(`CliMessages` の i18n 未対応)
- mvvm のガイド文章はレガシー流用(構造のみ新形式)
- `create` → `doc init` の自動連携(§17)
- feature プリセットの中身(auth/payment)・複数AIランタイム・コマンドホワイトリスト(D8 のまま)
