# utakata v1.0.0: 仕様駆動開発＆AIオーケストレーション基盤

## 1. コンセプト・ビジョン
従来の utakata は、開発者がクリーンアーキテクチャ等の雛形を素早く生成するための「CLIジェネレーター」でした[cite: 1]。
v1.0.0 では、この枠組みを大きく破壊・再構築します。
新しい utakata は、「クライアント（顧客）」「開発者（人間）」「複数のAIエージェント」という異なるアクターが、一つのプロジェクトで安全かつ確実に協働するためのプロジェクト・オーケストレーターへと進化します。
アーキテクチャの知識（ナレッジ）をプロジェクトから完全に分離し、コンテキストの肥大化を防ぎつつ、AIに対して「絶対に守るべきルール」と「実装すべき機能」を明確に区別して与える世界を実現します。

## 2. 目玉機能①：プロジェクトの中枢となる utakata.yaml
プロジェクトルートに配置される utakata.yaml が、プロジェクト全体のルールと座組を定義するマスターファイルになります。これにより、人間もAIも「このプロジェクトがどういうルールで、誰の決定で動いているか」を即座に把握できます。

### A. ナレッジリポジトリの外部化とコンテキストの分離
アーキテクチャのガイドや絶対遵守のルール（principles/, layers/ など）をプロジェクトから切り離し、外部のリポジトリで一元管理します。
これにより、各プロジェクトのコンテキスト（AIが読み込むべきプロンプト量）が大幅に削減・最適化されます。
*   **デフォルト参照先**: `https://github.com/utakata-code/utakata_arch_lib`
*   **オーバーライド**: ユーザー固有のオレオレアーキテクチャや社内標準ルールがある場合は、Git URL を指定するだけで簡単に独自のルールセットをAIに読み込ませることができます。

### B. 登場人物（アクター）とロールの定義
プロジェクトに関わる人間とAIの役割を明記し、AIエージェントに「誰の言うことを聞き、誰に判断を仰ぐべきか」という強力なコンテキストを与えます。

### C. SKILLs（AIエージェントのスキル）の動的解決と自動追記
プロジェクトやアーキテクチャ固有の「AIの振る舞い（SKILL）」を定義します[cite: 1]。
CLIがアーキテクチャをリモートからフェッチした際、そのアーキテクチャに同梱されている専用の SKILLs（例: clean_arch_auditor）が `.claude/skills/` 等に自動で解決・追記され、AIがそのプロジェクトに最適な立ち回りを即座に習得します[cite: 1]。

#### utakata.yaml の記述イメージ
```yaml
# utakata.yaml
project:
  architecture: clean_architecture
  knowledge_repo: 
    url: "[https://github.com/utakata-code/utakata_arch_lib.git](https://github.com/utakata-code/utakata_arch_lib.git)"
    ref: "v1.0.0"
  
  # アーキテクチャフェッチ時にリモートから自動解決・追記されるAIスキル (独自の追加や、アーキテクチャから紐づいてきたものの手動解除に対応)
  skills:
    - utakata-structure
    - clean-arch-auditor

team:
  client: "山田さん（要件の決定権者。仕様変更はこの人の合意が必要）"
  developer: "私（アーキテクチャの責任者。コードの最終レビューを行う）"
  ai_agents:
    - id: "feature-builder"
      role: "実装担当。plan.yaml と層ごとのガイドを読み込みコードを生成する。"
    - id: "structure-auditor"
      role: "監視担当。utakata check を駆使し、構造違反を検知して人間に報告する。"

```

## 3. 目玉機能②：大規模案件向け「機能別」進捗管理（feature ファーストは前提）

従来の「巨大な1枚の実装計画書」では、大規模案件になるとAIがコンテキストを読み切れずハルシネーションを起こす問題がありました。これを解決するため、フィーチャー（機能）ごとの実装計画ライフサイクル管理を導入します。

### utakata impl による自律的な進行



機能ごとに `doc/impl/PLAN-NNNN_{feature}.md` という独立した計画書（フロントマターに構造化データを内包し、Markdownをラッパーとした形式）を発行し、管理します。

* **状態遷移**: `Draft` -> `In Progress` -> `Done` -> `Archived` のように、機能ごとにマイルストーンを管理します。


* **文脈の隔離**: 実装中のAIとの試行錯誤（プロンプトや技術選定の理由）は、この機能別 PLAN ファイル内に書き込ませることで、他の機能の実装時にAIが余計な文脈に引きずられるのを防ぎます（コンテキストの完全な分断）。
* **履歴の保管**: 実装が完了（Done）したものは `doc/impl/archive/` に退避され、現在のワークスペースを常にクリーンに保ちます。



## 4. 目玉機能③：対クライアントのビジネスログの構造化

構造化データ（.json, .yaml）をマスター基準とし、人間の視認性を優先した .md をラッパーに据える形で管理します。AIとの作業ログ（`doc/impl/`）とは完全に隔離し、「なぜこの仕様になったのか」というビジネス上の絶対的な決定事項を安全に管理します。

* **会話ログ (utakata log)**: クライアントや開発者の重要な発言を `doc/records/log/YYYY-MM.jsonl` に追記型で記録し、AIに「実装のビジネス的背景」を理解させます。


* **合意形成 (utakata agree)**: 金額や仕様の合意事項を `doc/records/agreements.jsonl` に記録します。


* **AIの書き込み禁止（ホストレベルの防御）**: これらの `doc/records/` 領域は人間専用（human-write-only）とし、AIエージェントの書き込みをシステムレベル（`.claude/settings.json` の deny ルールなど）で厳格にブロックすることで、ビジネスの正史がAIによって改ざんされるのを防ぎます。



## 5. 複数AIの活用とコスト最適化

複数のAIエージェントを使い分ける座組（`team`）を構築し、タスクの粒度や役割に応じて適切なAIをオーケストレーションすることで、推論コストの最適化と精度の最大化を同時に図ります。

## 6. 開発ロードマップ（次なるステップ）

### A. ナレッジリポジトリ（utakata_arch_lib）の構築

* `.tmpl` を廃止し、`principles/` と `layers/` に責務を絞った美しいディレクトリツリーを作成する。
* アーキテクチャ固有の AI SKILLs（マークダウン）を各アーキテクチャディレクトリに同梱する。

### B. CLI（utakata）側のパース・フェッチエンジンの実装

* `utakata.yaml` の定義に基づき、リモートリポジトリから指定バージョンのナレッジとSKILLsを `~/.utakata/` にキャッシュする `utakata arch get` コマンドの実装。

### C. MCPサーバーの拡張

* AIエージェントが、キャッシュされたリモートガイドや自動追加されたSKILLsをシームレスに読み出せるように `utakata mcp` サーバーの `guide_get` ツール等を拡張する。


* **フック機能の目玉**: リンター（`flutter analyze`）等からエラーが出た際、そのリントエラーの発生場所から該当レイヤーのファイルのガイド（.md）を決定論的に自動参照し、即座に修正のコンテキストとしてAIに食わせる仕組みの構築。



### D. Claude Code連携の強化

* `user/claude/` に存在するクロードとの公式な会話ログを、プロジェクトのディレクトリにシームレスに統合・同期する仕組み。
* AIが実行した過去のコマンド履歴等を大局的に分析し、プロジェクト内で安全・確実に機能する「推奨コマンドの策定（ホワイトリスト形式）」を自動で行う機能。