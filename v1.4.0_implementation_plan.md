# v1.4.0 実装計画 — Issue #13 / #15 + コマンド品質

作成日: 2026-08-06
対象 Issue: [#13 utakata apply(GUIDE.md 生成)](https://github.com/utakata-code/utakata/issues/13) / [#15 純粋な dart cli ツールへ](https://github.com/utakata-code/utakata/issues/15)
リリース: 1件にまとめて **v1.4.0**(すべて後方互換のため)

## 決定事項

| # | 決定 | 理由 |
|---|---|---|
| D1 | GUIDE.md は**完全に生成停止**(オプトインも作らない) | v1.0.0 の「ナレッジはプロジェクトにコピーしない」方針に apply だけが違反していた。`guide for <file>` が正確な代替。手元に置きたい人には `guide eject` が既にある |
| D2 | 既存プロジェクトの GUIDE.md は**自動削除しない**。`doctor` が件数を報告し、削除可と案内する | 手で編集されている可能性があるため。scan は .dart のみ対象なので check への影響はゼロ(確認済み) |
| D3 | 同梱は **`arch_definition.yaml` + `skills/` のみ**(44KB)。読み物(layers/principles/dependencies = 584KB)は同梱をやめる | 構造コマンド(create/apply/check/feature add)は定義だけで完全動作 = オフライン維持。読み物は参照時のみ必要 |
| D4 | 読み物の正本は**既存の `utakata_arch_lib`**(新リポジトリは作らない)。`arch_definition.yaml` の `detail_content_path` が layers/ を指すため、定義と読み物は同一リポジトリに同居する必要がある | 現状の同梱物は arch_lib の複製にすぎない(1バイト単位で一致を確認済み) |
| D5 | `knowledge_repo` 未指定時の**既定値を公式 arch_lib + 固定タグ**とし、ガイド参照時に**自動フェッチ**する(グローバルキャッシュ、lock ファイルはプロジェクトに書かない) | main 追従は arch_lib 側の変更で過去 CLI が壊れる。タグは CLI バージョンごとにコードへ埋め込む。オプトインの `knowledge_repo` は従来どおり utakata.lock を使う |
| D6 | フェッチ失敗(オフライン・git 不在)時、**構造コマンドは影響なし**。ガイド系のみ「ネットワークが必要」と明示エラー | 受託現場のオフラインリスクを構造操作から完全に切り離す |
| D7 | ハードコード日本語の説明文 18 箇所 + 新コマンドの主要メッセージを `CliMessages` へ移し、`plan`/`diff`/`feature` の嘘の説明文(削除済みファイル参照)を修正 | 英語環境で日本語が混ざる不整合の解消 |

## 実装フェーズ

- **A(品質)**: en/ja メッセージ追加(~30 getter)、18 箇所の説明文を `_msg` 経由に、stale な 3 説明文の修正
- **B(#13)**: `AddFeatureUsecase` から `GenerateGuidesUsecase` を除去(usecase 自体と bin 配線も削除)。`FilesystemDataSource.listFilesNamed` を追加し `doctor` が残存 GUIDE.md を報告
- **C(#15)**: `tool/sync_arch_lib.dart` を slim 同梱(定義+skills)に変更 → バンドル再生成。`KnowledgeRepositoryImpl.ensureDefaultAvailable()`(既定リポジトリの取得・グローバルキャッシュ・sha マーカー)。`bin` の `resolveTemplatePath` を「①設定済みリモート → ②同梱(存在すれば) → ③既定キャッシュ(無ければ自動フェッチ)」に拡張。`arch get` は未設定時に既定リポジトリを取得する動作へ
- **D(検証)**: 全テスト + e2e(EN 環境の --help に日本語が混ざらない / slim 同梱で check・apply がネットワークなしで動く / guide show が自動フェッチで動く)+ 敵対的検証ワークフロー
- **E(リリース)**: README×2・doc/utakata-yaml.md・CHANGELOG 更新、publish、#13/#15 クローズ

## リスクと対処

- **arch_lib のタグずれ**: 既定タグは `v1.0.0`(現 main と同一内容)。arch_lib を更新したら新タグを打ち、CLI 側の定数を更新してリリースする運用
- **`GenerateGuidesUsecase` 削除の影響**: 利用箇所は AddFeatureUsecase のみ(確認済み)。guide show/for は別経路(GuideUsecase)で無影響
- **自動フェッチの UX**: 初回のみ「公式ナレッジを取得しています…」を stderr に表示。以後はキャッシュで無音
