# `v1.0.0.md` について — レビュー意見

対象: `v1.0.0.md`(コンセプト・ビジョン文書)
参照: `packages/utakata_arch_lib`(クローン済み・実体を確認した上での意見)

---

## 総評

§3(feature別実装計画)・§4(記録の構造化)は、この会話で実装した`utakata impl`/`utakata log`/`utakata agree`とほぼそのまま一致しており、`utakata.yaml`というマスター設定ファイルの発想も既に`doc init`で実在します(現状は`project.architecture`/`enforcement.impl_plan`/`records.git`/`lang`のみ)。つまりこのビジョンは全面的な作り直しではなく、**土台の上に「知識の外部化」「アクター定義」「複数AI編成」を足す話**です。土台が正しかったことの裏付けとして評価しています。

一方で、目玉である「ナレッジリポジトリの外部化」(§2-A)は、既存の設計判断(P1: ファイルシステムが唯一の真実、参照化=配布しないことによる保護)と逆方向に振れる、コストの高い飛躍です。以下、懸念点を優先度が高い順に挙げます。

---

## 懸念点

### ① リモートナレッジフェッチが一番大きな飛躍

現状`utakata`にはネットワークアクセスが一切なく(git/httpフェッチの仕組みはゼロ)、ガイド・テンプレートは全てパッケージに同梱されています。これを外部Gitリポジトリ参照に変えると:

- **オフライン・再現性の喪失**: 受託案件でクライアント環境がネットワーク制限されているケースは普通にある。`utakata create`がネット到達性に依存するようになる。
- **サプライチェーン注入経路**: §2-Cの「SKILLsの自動追記」と組み合わさると、リモートから取得したMarkdownがそのまま`.claude/skills/`に書き込まれ、AIエージェントへの指示として実行されます。`ref`でバージョン指定するとありますが、コミットSHA固定やチェックサム検証がなければ「差し替えられた知識をAIが無条件に信じる」経路になります。
- **ロールバックの喪失**: ローカルスナップショット(lockfile相当)がないと、`utakata_arch_lib`側が後から変わった時に過去プロジェクトの再現性が壊れます。

> **追記(2026-07-18)**: ユーザーからのフィードバックを受けて訂正。デフォルトの参照先は「今と同じくutakataパッケージ自体に同梱」でよく、`dart pub global activate`の時点で既にpub cacheへローカルにダウンロード済みになるため、**デフォルト経路には追加のネットワーク依存は発生しない**(オフライン・再現性の懸念はここには当てはまらない)。`knowledge_repo.url`は、社内標準やオレオレアーキテクチャなど**オーバーライドしたい時だけ**明示的に指定するオプトインの経路として設計する — その場合に限り、上記のオフライン依存・サプライチェーン注入・ロールバック喪失の懸念がそのまま適用される。つまりこの節の懸念は「デフォルト」ではなく「明示的にオーバーライドを選んだ場合」に絞られる。

### ② SKILLsの自動追記に競合解決の設計がない

再フェッチ時、ユーザーが`.claude/skills/`を手編集していた場合にどうするか(サイレント上書き/diff提示/スキップ)が書かれていません。以前の`guide eject`(単純コピー、ハッシュ管理なし)と同じ「配布しない・上書きしない」の思想を、ここにも適用する必要があります。

### ③ `team.ai_agents`の複数AI編成は実装計画がロードマップに存在しない

§5(複数AIのコスト最適化)には、ロードマップA〜Dのどこにも対応する実装項目がありません。ビジョンとしては魅力的ですが、実際のユースケース(フリーランス受託、基本1人+クライアント+AI)に今どこまで必要かは要検討です。過去の合意プリセット(auth/payment)の判断 — 「実案件2件で実証されるまで保留」 — と同じ扱いにする方が安全に見えます。

### ④ `.tmpl`廃止・`principles/`/`layers/`再設計は、それ単体で1バージョン分の規模

v1.1で仕上げたテンプレート体系の作り直しに相当します。「v1.0.0リリース」に混ぜ込むとスコープが膨らみすぎます。

### ⑤ `utakata_arch_lib`の現状は、実は今回の再設計より前の内容のコピーである(要修正)

実際にクローンして中身を確認したところ、次の不整合がありました:

- `arches/mvvm/`は**手つかず**で、`.agent/`・`AI/architecture/`・`AI/logs/conversation_log.md`・`AI/specs/feature_request.yaml`と、今回の会話で明確に廃止した旧構成がそのまま入っています(v1.1で`.agent/`同梱停止、`feature_request.yaml`は`plan.yaml`への移行対象、`AI/logs/`は`doc/records/log/`へ統合対象)。
- `arches/clean_architecture/`は`dependencies/`切り出し・`layers/`への改名は進んでいますが、`.tmpl`ファイル自体は引き続き存在しており(`entity.dart.tmpl`など)、ロードマップA「`.tmpl`を廃止」はまだ未着手です。
- `arches/clean_architecture/layers/features/ARCHITECTURE.md`は、`generate_feature.sh`/`generate_feature.ps1`という**存在しないシェルスクリプト**を前提にした説明のままです(これらはCLIコマンド`utakata feature add`/`apply`に置き換わって久しく、utakata_code側では今回のv1.1で完全に削除済み)。

つまり、今この状態で「リモートフェッチしてAIに読ませる」を実装すると、**現行CLIと矛盾する古い運用をAIに注入する**ことになります。ナレッジリポジトリの中身をv1.0.0の実装内容に合わせて同期するのが、フェッチ機構の実装より先に必要です。

> **追記(2026-07-18)**: ユーザーからの補足。`utakata_arch_lib`は**リポジトリ構造自体がまだ検討中(WIP)**であり、上記の不整合は「バグ」ではなく「未完成な現状」。`packages/`配下へのクローンは、この未完成な状態を筆者(Claude)にも把握させる目的だった。最終的にこのリポジトリに置く想定は最低限次の3種類:
> 1. Clean Architecture テンプレート(既存)
> 2. MVVM テンプレート(既存、要整備)
> 3. **ユーザー自身が独自のアーキテクチャを追加できる「テンプレート雛形」**(空の骨組み — `arch_definition.yaml`やレイヤー構成をゼロから定義するためのスターターキット)
>
> これをパブリックに公開することで、誰でも自分用にカスタマイズ/独自アーキテクチャを追加できるようにする狙い。

### ⑥ Claude公式ログ同期はプライバシー境界の設計が必要

`user/claude/`配下の公式会話ログはセッション横断で他クライアントの情報が混ざっている可能性があり、無差別に1プロジェクトへ同期すると受託案件の秘密保持上の事故になり得ます。プロジェクト/セッション単位のスコープ限定を先に設計すべきです。

> **追記(2026-07-18)**: 公式ドキュメント(`https://code.claude.com/docs/en/memory`)を確認。Claude Codeには既に`/memory`(Memory機能)が存在し、この懸念の一部は前提が変わる:
> - **CLAUDE.md**(user: `~/.claude/CLAUDE.md` / project: `./CLAUDE.md` / local: `./CLAUDE.local.md`)は人間が書く「指示・ルール」であり、会話の生ログではない。project levelはgit管理・チーム共有前提、local levelは`.gitignore`対象。
> - **auto memory**(`~/.claude/projects/<project>/memory/`、`MEMORY.md`+トピックファイル)はClaude自身が「将来の会話で役立ちそうか」を判断して書く**キュレーション済みの学習内容**であり、生の会話ログそのものではない。**プロジェクト単位(gitリポジトリ由来のパス)で厳密にスコープされる**ため、当初懸念していた「他クライアントの情報が混ざる」は、生ログ同期を独自実装した場合の懸念であって、Memory機能自体には(ドキュメント上は)当てはまらない。ただし「machine-local」であり、マシン間・チーム間では共有されない。
> - 一方で、生セッションのJSONLトランスクリプト(`~/.claude/projects/<project>/*.jsonl`)自体はMemory機能とは**別物**で、ドキュメントにも同期の仕組みは記載がなく、機微情報の自動レダクションも文書化されていない。
>
> **示唆**: ロードマップDが本当に欲しいもの(セッションをまたいだプロジェクト文脈の永続化)は、生ログを同期する独自機構を作らなくても**Claude Codeの`auto memory`が既にプロジェクト単位で提供している**。utakata側でやる価値があるとすれば、生成する project-level `CLAUDE.md` に「`doc/summary.md`・`doc/specs/plan.yaml`・`doc/records/`を見よ」といった導線を最初から書いておくことだけで十分な可能性が高い。「生ログの同期」自体は優先度を下げてよい。
>
> **再訂正(2026-07-18)**: ユーザーから訂正あり。ロードマップDが指すのは`auto memory`(キュレーション済みメモ)ではなく、**Claude Codeのセッション履歴そのもの**(ユーザー発言とAIエージェント応答が1ターンずつJSON/JSONLで保存されている生トランスクリプト、`~/.claude/projects/<project>/*.jsonl`)。これを**`doc/records/`配下に同期し、Gitでバージョン管理したい**、という要望。目的は「AIへのコンテキスト供給」ではなく、**AIとのやり取りをコードやdoc/と同じ粒度でGit管理下に置く監査証跡**。
>
> この理解のもとでの設計論点:
> - **人間駆動の同期**: `doc/records/`は「人間が書き、AIは読むだけ」が既存の原則(`.claude/settings.json`のdenyルールで技術的に強制)。セッションJSONの同期も、Claude自身がその場でファイルを書くのではなく、**人間が明示的にコマンドを叩いて取り込む**形にすれば、この原則と矛盾しない(`utakata log add`と同じパターン)。
> - **プロジェクト単位の分離は既に解決済み**: `<project>`パスはgitリポジトリ由来で分離されるため、他クライアントの情報混入という当初の懸念は当たらない(2026-07-18の前の追記のとおり)。
> - **未解決の論点**: (a) 機微情報のレダクション(トラブルシューティングで環境変数・認証情報を貼った場合の扱い)、(b) thinkingブロックやツール呼び出しの生JSONによるノイズ・リポジトリ肥大化、(c) 保存形式(生JSONLそのままか、`doc/records/log`と同様に人間可読なプレビューを併置するか)。これらは実装前に決めるべき。

---

## 推奨する進め方

1. **今すぐやる価値がある**: `utakata.yaml`に`team`/`skills`フィールドを足す(ローカル完結、リスクなし)。
2. **先にやるべき**: `utakata_arch_lib`の中身を、今回のv1.0.0(旧0.6〜1.1)の再設計内容に同期する(mvvmの`.agent`/レガシー構成の一掃、`.tmpl`廃止の完遂、シェルスクリプト前提の記述の除去)。リモートフェッチを実装する前に、フェッチ対象そのものが正しい状態である必要があります。
3. **別バージョンとして切り出す**: リモートフェッチ機構(ロードマップA・B・C)は、バージョン固定+チェックサム検証を伴う最小実装から始める。
4. **設計してから着手**: ロードマップDの「公式ログ同期」は、`auto memory`とは別物で、**セッション生トランスクリプトを`doc/records/`配下にGit管理下で取り込む**という独立した要望であることが確定した(2026-07-18追記参照)。`utakata log add`と同じ「人間がコマンドを叩いて取り込む」形にすれば既存の human-write-only原則と矛盾しない。ただしレダクション・ノイズ削減・保存形式の3点を先に決める必要がある。コマンド白紙化案(過去のコマンド履歴分析)も引き続きスコープ限定の設計をしてから着手する。

---

## 未確定事項

- pubspecのバージョンは`1.0.0`に変更済み・`utakata_arch_lib`は`packages/utakata_arch_lib/`にクローン済み(`.gitignore`済み、別リポジトリとして独立管理)。
- §5(複数AI編成)を今回のv1.0.0スコープに含めるか、次バージョン以降に回すかは未決定。
- `utakata_arch_lib`の同期(懸念⑤)をv1.0.0の作業に含めるか、知識リポジトリ側の別タスクとして切り離すかも未決定。
