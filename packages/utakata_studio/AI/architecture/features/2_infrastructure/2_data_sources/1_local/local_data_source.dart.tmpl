// {{EntityName}} ローカルデータソース
//
// インフラ層のローカルデータソース。
// SQLite / SharedPreferences / ローカルファイル等からデータを取得する。

import '../../1_models/{{entity_name}}_model.dart';

abstract interface class {{EntityName}}void LocalDataSource {
  /// ID で取得する
  Future<{{EntityName}}Model?> findById(String id);

  /// 全件取得する
  Future<List<{{EntityName}}Model>> findAll();

  /// 保存する
  Future<void> save({{EntityName}}Model model);

  /// 削除する
  Future<void> delete(String id);
}
