// {{EntityName}} エンティティ
//
// ドメイン層のエンティティ。ビジネスロジックの中心となるデータ構造。
// freezed を使用して不変性を保証する。

import 'package:freezed_annotation/freezed_annotation.dart';

part '{{entity_name}}_entity.freezed.dart';

@freezed
abstract class {{EntityName}}Entity with void _${{EntityName}}void Entity {
  const factory {{EntityName}}Entity({
    required String id,
    // TODO: フィールドを追加してください
  }) = _{{EntityName}}Entity;

  const {{EntityName}}Entity._();
}
