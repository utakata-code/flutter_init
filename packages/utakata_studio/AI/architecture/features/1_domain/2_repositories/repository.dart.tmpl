// {{EntityName}} リポジトリ インターフェース
//
// ドメイン層のリポジトリ定義。
// 実装は 2_infrastructure/3_repositories/ に存在する。

import '../1_entities/{{entity_name}}_entity.dart';

abstract interface class {{EntityName}}void Repository {
  /// {{EntityName}} を ID で取得する
  Future<{{EntityName}}Entity?> findById(String id);

  /// 全件取得する
  Future<List<{{EntityName}}Entity>> findAll();

  /// 保存する
  Future<void> save({{EntityName}}Entity entity);

  /// 削除する
  Future<void> delete(String id);
}
